#!/usr/bin/env python

from setuptools import setup

setup(
	name = 'numcrypt',
	description = 'Utility library for textual data visualization',
	url = 'https://github.com/Ekvium/numCrypt',
	packages = ['numcrypt'],
)
